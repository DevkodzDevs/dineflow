# Each app keeps its own React (Expo 52 → React 18, Next 15 → React 19), while a flat root
# node_modules lets Metro find transitive helpers. This is the standard layout for Expo monorepos.
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=false
